import { EasterEditor } from "@/components/timeline/EasterEditor";

/** Renders the hidden communication graph animation editor. */
export default function EasterEditorPage() {
  return <EasterEditor />;
}
