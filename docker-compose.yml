services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: llm_oj
      POSTGRES_PASSWORD: llm_oj
      POSTGRES_DB: llm_oj
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U llm_oj"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
